name: delta_ms_toy
channels:
  - conda-forge
  - defaults
dependencies:
  - ipywidgets
  - matplotlib
  - jupyterlab
  - ipython
  - pip
  - pip:
    - rsmf==0.2
prefix: /opt/miniconda3/envs/delta_ms_toy
